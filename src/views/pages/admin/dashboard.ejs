<!-- Admin Dashboard -->
<div class="space-y-8">
  <!-- Stats Grid -->
  <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
    <div class="glass rounded-2xl p-6 border border-white/10">
      <div class="flex items-center gap-4">
        <div class="w-12 h-12 rounded-xl bg-green-500/20 flex items-center justify-center text-2xl">
          💰
        </div>
        <div>
          <p class="text-2xl font-bold">R$ <%= stats.balance.toFixed(2) %></p>
          <p class="text-sm text-gray-400">Saldo atual</p>
        </div>
      </div>
    </div>
    <div class="glass rounded-2xl p-6 border border-white/10">
      <div class="flex items-center gap-4">
        <div class="w-12 h-12 rounded-xl bg-blue-500/20 flex items-center justify-center text-2xl">
          🎁
        </div>
        <div>
          <p class="text-2xl font-bold"><%= stats.donations %></p>
          <p class="text-sm text-gray-400">Doações recebidas</p>
        </div>
      </div>
    </div>
    <div class="glass rounded-2xl p-6 border border-white/10">
      <div class="flex items-center gap-4">
        <div
          class="w-12 h-12 rounded-xl bg-purple-500/20 flex items-center justify-center text-2xl"
        >
          🗳️
        </div>
        <div>
          <p class="text-2xl font-bold"><%= stats.proposals %></p>
          <p class="text-sm text-gray-400">Propostas ativas</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Quick Actions -->
  <div class="glass rounded-2xl p-6 border border-white/10">
    <h2 class="text-lg font-semibold mb-4">⚡ Ações Rápidas</h2>
    <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
      <a
        href="/admin/updates"
        class="p-4 bg-white/5 rounded-xl hover:bg-white/10 transition text-center"
      >
        <span class="text-2xl block mb-2">📝</span>
        <span class="text-sm">Novo Update</span>
      </a>
      <a
        href="/admin/layout"
        class="p-4 bg-white/5 rounded-xl hover:bg-white/10 transition text-center"
      >
        <span class="text-2xl block mb-2">🎨</span>
        <span class="text-sm">Editar Layout</span>
      </a>
      <a
        href="/admin/proposals"
        class="p-4 bg-white/5 rounded-xl hover:bg-white/10 transition text-center"
      >
        <span class="text-2xl block mb-2">🗳️</span>
        <span class="text-sm">Ver Propostas</span>
      </a>
      <a
        href="/"
        target="_blank"
        class="p-4 bg-white/5 rounded-xl hover:bg-white/10 transition text-center"
      >
        <span class="text-2xl block mb-2">🌐</span>
        <span class="text-sm">Ver Site</span>
      </a>
    </div>
  </div>

  <!-- Modules Toggle -->
  <div class="glass rounded-2xl p-6 border border-white/10">
    <h2 class="text-lg font-semibold mb-4">🧩 Módulos</h2>
    <div class="grid grid-cols-2 md:grid-cols-3 gap-4">
      <% Object.entries(modules).forEach(([key, mod]) => { %>
      <label
        class="flex items-center gap-3 p-3 bg-white/5 rounded-xl cursor-pointer hover:bg-white/10 transition"
      >
        <input type="checkbox" <%= mod.enabled ? 'checked' : '' %> hx-post="/admin/modules/toggle"
        hx-vals='{"module": "<%= key %>", "enabled": <%= !mod.enabled %>}' hx-swap="none" class="w-5
        h-5 rounded accent-primary" />
        <span class="capitalize"><%= key %></span>
      </label>
      <% }); %>
    </div>
  </div>

  <!-- Recent Proposals -->
  <% if (proposals.length > 0) { %>
  <div class="glass rounded-2xl p-6 border border-white/10">
    <h2 class="text-lg font-semibold mb-4">🗳️ Propostas Recentes</h2>
    <div class="space-y-3">
      <% proposals.slice(0, 5).forEach(p => { %>
      <div class="flex items-center justify-between p-3 bg-white/5 rounded-xl">
        <div>
          <p class="font-medium"><%= p.title %></p>
          <p class="text-sm text-gray-400">Por <%= p.authorHandle %></p>
        </div>
        <div class="flex items-center gap-2 text-sm">
          <span class="text-green-400">👍 <%= p.yesCount %></span>
          <span class="text-red-400">👎 <%= p.noCount %></span>
        </div>
      </div>
      <% }); %>
    </div>
  </div>
  <% } %>
</div>
