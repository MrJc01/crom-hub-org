<!-- Full Transparency Page -->
<div class="mb-8">
  <h1 class="text-2xl font-bold mb-2">📊 Transparência Financeira</h1>
  <p class="text-gray-400">Todas as entradas e saídas são públicas.</p>
</div>

<!-- Summary -->
<section class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-8">
  <div class="bg-gray-800 rounded-xl p-4 border border-gray-700">
    <div class="text-sm text-gray-400 mb-1">Saldo Atual</div>
    <p class="text-2xl font-bold text-white">
      <%= currency %> <%= balance.toLocaleString('pt-BR', {
      minimumFractionDigits: 2 }) %>
    </p>
  </div>
  <div class="bg-gray-800 rounded-xl p-4 border border-gray-700">
    <div class="text-sm text-gray-400 mb-1">Total Arrecadado</div>
    <p class="text-2xl font-bold text-green-400">
      +<%= currency %> <%= totalIn.toLocaleString('pt-BR', {
      minimumFractionDigits: 2 }) %>
    </p>
  </div>
  <div class="bg-gray-800 rounded-xl p-4 border border-gray-700">
    <div class="text-sm text-gray-400 mb-1">Total Gasto</div>
    <p class="text-2xl font-bold text-red-400">
      -<%= currency %> <%= totalOut.toLocaleString('pt-BR', {
      minimumFractionDigits: 2 }) %>
    </p>
  </div>
</section>

<!-- All Transactions -->
<section>
  <h2 class="text-lg font-semibold mb-4">Todas as Transações</h2>
  <%- include('../partials/transactions-list', { transactions }) %>
</section>
