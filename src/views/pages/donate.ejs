<main class="pt-24 pb-12">
  <div class="max-w-2xl mx-auto px-6">
    <div class="text-center mb-12">
      <span
        class="inline-block px-3 py-1 rounded-full bg-pink-500/20 text-pink-500 text-sm font-medium mb-4"
      >
        💖 Apoie
      </span>
      <h1 class="text-4xl font-bold mb-4">Faça a Diferença</h1>
      <p class="text-gray-400">
        Sua contribuição ajuda a manter nossa infraestrutura e independência.
      </p>
    </div>

    <div class="glass rounded-xl p-8 border border-white/10 shadow-2xl">
      <%- include('../partials/donation-form', { error: null }) %>
    </div>

    <!-- Rewards Info -->
    <% if (modules.donations.settings.rewards && modules.donations.settings.rewards.length > 0) { %>
    <div class="mt-12 text-center">
      <h3 class="text-lg font-bold mb-6">Recompensas e Reconhecimento</h3>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <% modules.donations.settings.rewards.forEach(reward => { %>
        <div class="bg-gray-800/50 p-4 rounded-xl border border-gray-700">
          <div class="text-2xl font-bold text-<%= reward.color %>-400 mb-1">
            R$ <%= reward.amount %>+
          </div>
          <div class="font-medium text-white mb-1"><%= reward.tag %></div>
          <p class="text-xs text-gray-500">Ganhe a badge exclusiva no seu perfil</p>
        </div>
        <% }) %>
      </div>
    </div>
    <% } %>
  </div>
</main>
