<!-- Activity History -->
<div class="space-y-6">
  <div class="flex items-center justify-between">
    <h1 class="text-2xl font-bold">Minha Atividade</h1>

    <!-- Filter (Placeholder for now) -->
    <select
      class="bg-white/5 border border-white/10 rounded-xl px-4 py-2 text-sm focus:outline-none focus:border-primary"
    >
      <option value="all">Todas</option>
      <option value="votes">Votos</option>
      <option value="donations">Doações</option>
    </select>
  </div>

  <% if (activity.length === 0) { %>
  <!-- Empty State -->
  <div class="text-center py-20 opacity-50">
    <div class="text-4xl mb-4">🕸️</div>
    <p class="text-lg font-medium">Nenhuma atividade recente</p>
    <p class="text-sm">Suas ações aparecerão aqui.</p>
  </div>
  <% } else { %>
  <!-- Timeline -->
  <div
    class="relative pl-8 space-y-8 before:content-[''] before:absolute before:left-3 before:top-2 before:bottom-0 before:w-0.5 before:bg-white/10"
  >
    <% activity.forEach(item => { %>
    <div class="relative">
      <!-- Icon Point -->
      <div
        class="absolute -left-[35px] w-8 h-8 rounded-full border-4 border-slate-900 flex items-center justify-center bg-slate-800 z-10"
      >
        <% if (item.type === 'vote') { %>
        <span class="text-xs">🗳️</span>
        <% } else { %>
        <span class="text-xs">💰</span>
        <% } %>
      </div>

      <!-- Card -->
      <div class="glass rounded-xl p-4 border border-white/10 hover:border-white/20 transition">
        <div class="flex items-start justify-between gap-4">
          <div>
            <% if (item.type === 'vote') { %>
            <p class="font-medium text-blue-400 mb-1">Votou em Proposta</p>
            <p class="text-gray-300">
              Você votou
              <span
                class="<%= item.data.vote === 'yes' ? 'text-green-400' : 'text-red-400' %> font-bold"
                ><%= item.data.vote === 'yes' ? 'SIM' : 'NÃO' %></span
              >
              em "<%= item.data.proposal?.title || 'Proposta' %>"
            </p>
            <% } else { %>
            <p class="font-medium text-green-400 mb-1">Doação Recebida</p>
            <p class="text-gray-300">
              Você doou
              <span class="text-green-300 font-bold">R$ <%= item.data.amount.toFixed(2) %></span>
              para o projeto. Muito obrigado! 💖
            </p>
            <% } %>
          </div>
          <span class="text-xs text-gray-500 whitespace-nowrap">
            <%= new Date(item.date).toLocaleDateString() %>
          </span>
        </div>

        <% if (item.type === 'donation' && item.data.message) { %>
        <div class="mt-3 p-3 bg-white/5 rounded-lg text-sm text-gray-400 italic">
          "<%= item.data.message %>"
        </div>
        <% } %>
      </div>
    </div>
    <% }); %>
  </div>
  <% } %>
</div>
