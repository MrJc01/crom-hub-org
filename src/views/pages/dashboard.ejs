<% if (locals.showWhatsappPopup) { %>
<div
  id="wa-popup"
  class="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/80 backdrop-blur-sm"
>
  <div
    class="bg-gray-900 border border-white/20 rounded-2xl p-6 max-w-md w-full shadow-2xl relative animate-in fade-in zoom-in duration-300"
  >
    <div class="text-center">
      <div
        class="w-16 h-16 bg-green-500/20 text-green-400 rounded-full flex items-center justify-center mx-auto mb-4 text-3xl"
      >
        💬
      </div>
      <h3 class="text-xl font-bold mb-2">Conecte seu WhatsApp</h3>
      <p class="text-gray-400 text-sm mb-6">
        Receba atualizações importantes e recupere seu acesso mais facilmente. Deseja cadastrar
        agora?
      </p>

      <form hx-post="/api/user/update-whatsapp" hx-swap="innerHTML" class="space-y-4">
        <div>
          <input
            type="text"
            name="whatsapp"
            placeholder="55 11 99999-9999"
            class="w-full bg-black/30 border border-white/10 rounded-xl px-4 py-3 text-white focus:border-green-500 outline-none text-center text-lg tracking-wider"
            required
          />
        </div>
        <button
          type="submit"
          class="w-full py-3 bg-green-500 hover:bg-green-600 text-white font-bold rounded-xl transition"
        >
          Salvar WhatsApp
        </button>
      </form>

      <button
        onclick="document.getElementById('wa-popup').remove()"
        hx-post="/api/user/dismiss-whatsapp"
        hx-swap="none"
        class="mt-4 text-sm text-gray-500 hover:text-white transition w-full p-2"
      >
        Não perguntar novamente
      </button>
    </div>
  </div>
</div>
<% } %> <% /* Dynamic Layout Engine - Loops through sections_order from config */ %> <%
sections_order.forEach(function(sectionId) { %> <% const section = sections_data[sectionId]; %> <%
if (section && section.enabled) { %> <%- include('../partials/sections/' + sectionId, { settings:
section || {}, data, organization, modules, config }) %> <% } %> <% }); %>
