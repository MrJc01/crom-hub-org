<!-- ==================== HERO SECTION ==================== -->
<section
  id="hero"
  class="gradient-bg min-h-screen flex items-center justify-center pt-20 relative overflow-hidden"
>
  <div class="absolute inset-0 overflow-hidden">
    <div
      class="absolute -top-40 -right-40 w-80 h-80 bg-primary/20 rounded-full blur-3xl"
    ></div>
    <div
      class="absolute -bottom-40 -left-40 w-80 h-80 bg-purple-600/20 rounded-full blur-3xl"
    ></div>
  </div>

  <div class="max-w-7xl mx-auto px-6 py-20 relative z-10">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
      <div>
        <div
          class="inline-flex items-center gap-2 px-4 py-2 bg-white/10 rounded-full text-sm mb-6"
        >
          <span class="w-2 h-2 bg-green-400 rounded-full animate-pulse"></span>
          <span>Open Source & Self-Hosted</span>
        </div>

        <h1 class="text-4xl md:text-6xl font-bold mb-6 leading-tight">
          Transparência
          <span
            class="text-transparent bg-clip-text bg-gradient-to-r from-primary to-purple-400"
            >Radical</span
          >
          para Projetos que Importam
        </h1>

        <p class="text-xl text-gray-300 mb-8 max-w-lg">
          <%= organization.description %>
        </p>

        <div class="flex flex-wrap gap-4">
          <a
            href="#doar"
            class="px-8 py-4 bg-primary hover:bg-primary-dark rounded-xl font-semibold transition transform hover:scale-105"
            >💖 Fazer Doação</a
          >
          <a
            href="#transparencia"
            class="px-8 py-4 bg-white/10 hover:bg-white/20 rounded-xl font-semibold transition"
            >📊 Ver Finanças</a
          >
        </div>

        <div class="flex gap-8 mt-12">
          <div>
            <p class="text-3xl font-bold text-green-400">
              +R$ <%= totalIn.toLocaleString('pt-BR', {minimumFractionDigits:
              0}) %>
            </p>
            <p class="text-gray-400 text-sm">Arrecadado</p>
          </div>
          <div>
            <p class="text-3xl font-bold"><%= donationCount %></p>
            <p class="text-gray-400 text-sm">Apoiadores</p>
          </div>
          <div>
            <p class="text-3xl font-bold text-primary">100%</p>
            <p class="text-gray-400 text-sm">Transparente</p>
          </div>
        </div>
      </div>

      <div class="hidden lg:block">
        <div class="glass rounded-3xl p-8 border border-white/10 animate-float">
          <div class="flex items-center gap-3 mb-6">
            <div
              class="w-12 h-12 rounded-xl bg-gradient-to-br from-primary to-purple-600 flex items-center justify-center text-2xl"
            >
              💰
            </div>
            <div>
              <p class="font-semibold">Saldo Atual</p>
              <p class="text-gray-400 text-sm">Atualizado em tempo real</p>
            </div>
          </div>
          <p class="text-5xl font-bold mb-4">
            R$ <%= balance.toLocaleString('pt-BR', {minimumFractionDigits: 2})
            %>
          </p>
          <div class="flex gap-4 text-sm">
            <span class="text-green-400"
              >↑ R$ <%= totalIn.toLocaleString('pt-BR', {minimumFractionDigits:
              0}) %> entrada</span
            >
            <span class="text-red-400"
              >↓ R$ <%= totalOut.toLocaleString('pt-BR', {minimumFractionDigits:
              0}) %> saída</span
            >
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ==================== ABOUT SECTION ==================== -->
<section id="sobre" class="py-24 bg-slate-800/50">
  <div class="max-w-7xl mx-auto px-6">
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-4xl font-bold mb-4">O que é o Hub.org?</h2>
      <p class="text-xl text-gray-400 max-w-2xl mx-auto">
        Uma plataforma focada em três pilares: arrecadação, transparência e
        governança.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      <div class="glass rounded-2xl p-8 border border-white/10 text-center">
        <div
          class="w-16 h-16 rounded-2xl bg-green-500/20 flex items-center justify-center text-3xl mx-auto mb-6"
        >
          💸
        </div>
        <h3 class="text-xl font-semibold mb-3">Arrecadação</h3>
        <p class="text-gray-400">
          Receba doações de forma simples. Sem taxas de plataforma.
        </p>
      </div>
      <div class="glass rounded-2xl p-8 border border-white/10 text-center">
        <div
          class="w-16 h-16 rounded-2xl bg-blue-500/20 flex items-center justify-center text-3xl mx-auto mb-6"
        >
          📊
        </div>
        <h3 class="text-xl font-semibold mb-3">Transparência</h3>
        <p class="text-gray-400">Todas as entradas e saídas são públicas.</p>
      </div>
      <div class="glass rounded-2xl p-8 border border-white/10 text-center">
        <div
          class="w-16 h-16 rounded-2xl bg-purple-500/20 flex items-center justify-center text-3xl mx-auto mb-6"
        >
          🗳️
        </div>
        <h3 class="text-xl font-semibold mb-3">Governança</h3>
        <p class="text-gray-400">
          Permita que sua comunidade participe de decisões.
        </p>
      </div>
    </div>
  </div>
</section>

<!-- ==================== FEATURES SECTION ==================== -->
<section id="funcionalidades" class="py-24">
  <div class="max-w-7xl mx-auto px-6">
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-4xl font-bold mb-4">Módulos Habilitados</h2>
      <p class="text-xl text-gray-400">
        Funcionalidades ativas nesta instância
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
      <div
        class="rounded-2xl p-6 border <%= modules.donations?.enabled ? 'bg-green-500/10 border-green-500/30' : 'bg-gray-800/50 border-gray-700' %>"
      >
        <div class="flex items-center justify-between mb-4">
          <span class="text-2xl">💰</span>
          <span
            class="px-2 py-1 rounded-full text-xs <%= modules.donations?.enabled ? 'bg-green-500/20 text-green-400' : 'bg-gray-700 text-gray-400' %>"
            ><%= modules.donations?.enabled ? 'Ativo' : 'Inativo' %></span
          >
        </div>
        <h3 class="font-semibold mb-2">Doações</h3>
        <p class="text-sm text-gray-400">Receba contribuições</p>
      </div>

      <div
        class="rounded-2xl p-6 border <%= modules.voting?.enabled ? 'bg-purple-500/10 border-purple-500/30' : 'bg-gray-800/50 border-gray-700' %>"
      >
        <div class="flex items-center justify-between mb-4">
          <span class="text-2xl">🗳️</span>
          <span
            class="px-2 py-1 rounded-full text-xs <%= modules.voting?.enabled ? 'bg-purple-500/20 text-purple-400' : 'bg-gray-700 text-gray-400' %>"
            ><%= modules.voting?.enabled ? 'Ativo' : 'Inativo' %></span
          >
        </div>
        <h3 class="font-semibold mb-2">Votação</h3>
        <p class="text-sm text-gray-400">Governança participativa</p>
      </div>

      <div
        class="rounded-2xl p-6 border <%= modules.transparency?.enabled ? 'bg-blue-500/10 border-blue-500/30' : 'bg-gray-800/50 border-gray-700' %>"
      >
        <div class="flex items-center justify-between mb-4">
          <span class="text-2xl">📊</span>
          <span
            class="px-2 py-1 rounded-full text-xs <%= modules.transparency?.enabled ? 'bg-blue-500/20 text-blue-400' : 'bg-gray-700 text-gray-400' %>"
            ><%= modules.transparency?.enabled ? 'Ativo' : 'Inativo' %></span
          >
        </div>
        <h3 class="font-semibold mb-2">Transparência</h3>
        <p class="text-sm text-gray-400">Dashboard público</p>
      </div>

      <div
        class="rounded-2xl p-6 border <%= modules.audit_log?.enabled ? 'bg-yellow-500/10 border-yellow-500/30' : 'bg-gray-800/50 border-gray-700' %>"
      >
        <div class="flex items-center justify-between mb-4">
          <span class="text-2xl">📋</span>
          <span
            class="px-2 py-1 rounded-full text-xs <%= modules.audit_log?.enabled ? 'bg-yellow-500/20 text-yellow-400' : 'bg-gray-700 text-gray-400' %>"
            ><%= modules.audit_log?.enabled ? 'Ativo' : 'Inativo' %></span
          >
        </div>
        <h3 class="font-semibold mb-2">Audit Log</h3>
        <p class="text-sm text-gray-400">Ações públicas</p>
      </div>
    </div>
  </div>
</section>

<!-- ==================== TRANSPARENCY SECTION ==================== -->
<section id="transparencia" class="py-24 bg-slate-800/50">
  <div class="max-w-7xl mx-auto px-6">
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-4xl font-bold mb-4">Transparência Total</h2>
      <p class="text-xl text-gray-400">Todas as movimentações são públicas</p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
      <div class="glass rounded-2xl p-6 border border-white/10">
        <p class="text-gray-400 text-sm mb-2">Saldo Disponível</p>
        <p class="text-4xl font-bold">
          <%= currency %> <%= balance.toLocaleString('pt-BR',
          {minimumFractionDigits: 2}) %>
        </p>
      </div>
      <div class="glass rounded-2xl p-6 border border-white/10">
        <p class="text-gray-400 text-sm mb-2">Total Arrecadado</p>
        <p class="text-4xl font-bold text-green-400">
          +<%= currency %> <%= totalIn.toLocaleString('pt-BR',
          {minimumFractionDigits: 2}) %>
        </p>
        <p class="text-sm text-gray-500 mt-1"><%= donationCount %> doações</p>
      </div>
      <div class="glass rounded-2xl p-6 border border-white/10">
        <p class="text-gray-400 text-sm mb-2">Total Gasto</p>
        <p class="text-4xl font-bold text-red-400">
          -<%= currency %> <%= totalOut.toLocaleString('pt-BR',
          {minimumFractionDigits: 2}) %>
        </p>
        <p class="text-sm text-gray-500 mt-1"><%= expenseCount %> gastos</p>
      </div>
    </div>

    <div class="glass rounded-2xl border border-white/10 overflow-hidden">
      <div
        class="p-6 border-b border-white/10 flex items-center justify-between"
      >
        <h3 class="font-semibold text-lg">Últimas Transações</h3>
        <a href="/transparency" class="text-primary hover:underline text-sm"
          >Ver todas →</a
        >
      </div>
      <div class="divide-y divide-white/5">
        <% if (transactions.length === 0) { %>
        <div class="p-8 text-center text-gray-400">
          Nenhuma transação ainda. Seja o primeiro a doar!
        </div>
        <% } else { %> <% transactions.slice(0, 5).forEach(function(tx) { %>
        <div
          class="p-4 flex items-center justify-between hover:bg-white/5 transition"
        >
          <div class="flex items-center gap-4">
            <div
              class="w-10 h-10 rounded-full flex items-center justify-center text-lg <%= tx.type === 'IN' ? 'bg-green-500/20' : 'bg-red-500/20' %>"
            >
              <%= tx.automatic ? '🤖' : (tx.type === 'IN' ? '💰' : '📤') %>
            </div>
            <div>
              <p class="font-medium">
                <%= tx.type === 'IN' ? (tx.donor || 'Anônimo') : (tx.description
                || 'Gasto') %>
              </p>
              <p class="text-sm text-gray-500">
                <%= new Date(tx.createdAt).toLocaleDateString('pt-BR') %>
              </p>
            </div>
          </div>
          <p
            class="font-bold <%= tx.type === 'IN' ? 'text-green-400' : 'text-red-400' %>"
          >
            <%= tx.type === 'IN' ? '+' : '-' %>R$ <%=
            tx.amount.toLocaleString('pt-BR', {minimumFractionDigits: 2}) %>
          </p>
        </div>
        <% }); %> <% } %>
      </div>
    </div>
  </div>
</section>

<!-- ==================== DONATION SECTION ==================== -->
<section id="doar" class="py-24">
  <div class="max-w-3xl mx-auto px-6">
    <div class="text-center mb-12">
      <h2 class="text-3xl md:text-4xl font-bold mb-4">Apoie o Projeto</h2>
      <p class="text-xl text-gray-400">
        Sua contribuição mantém o projeto vivo
      </p>
    </div>

    <div class="glass rounded-3xl p-8 border border-white/10">
      <div id="donation-form"><%- include('../partials/donation-form') %></div>
    </div>
  </div>
</section>

<!-- ==================== PHILOSOPHY SECTION ==================== -->
<section id="filosofia" class="py-24 bg-slate-800/50">
  <div class="max-w-4xl mx-auto px-6 text-center">
    <div
      class="inline-flex items-center gap-2 px-4 py-2 bg-white/10 rounded-full text-sm mb-6"
    >
      <span>🔒</span>
      <span>Filosofia Anti-Social</span>
    </div>

    <h2 class="text-3xl md:text-4xl font-bold mb-6">
      Foco em Transações, Não em Vaidade
    </h2>

    <p class="text-xl text-gray-400 mb-12">
      O Hub.org não é uma rede social. Usuários são identificados apenas por um
      <span class="text-primary font-mono">@handle</span> único.
    </p>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 text-left">
      <div class="p-6">
        <div class="text-2xl mb-3">🔐</div>
        <h3 class="font-semibold mb-2">Privacidade</h3>
        <p class="text-gray-400 text-sm">Nenhum dado pessoal exposto.</p>
      </div>
      <div class="p-6">
        <div class="text-2xl mb-3">🏠</div>
        <h3 class="font-semibold mb-2">Self-Hosted</h3>
        <p class="text-gray-400 text-sm">Seus dados no seu servidor.</p>
      </div>
      <div class="p-6">
        <div class="text-2xl mb-3">📖</div>
        <h3 class="font-semibold mb-2">Open Source</h3>
        <p class="text-gray-400 text-sm">100% código aberto.</p>
      </div>
    </div>
  </div>
</section>
