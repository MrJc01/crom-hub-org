<!-- Updates Section - Landing Page Partial -->
<section id="updates" class="py-20">
  <div class="max-w-6xl mx-auto px-6">
    <!-- Section Header -->
    <div class="text-center mb-12">
      <span
        class="inline-block px-3 py-1 rounded-full bg-primary/20 text-primary text-sm font-medium mb-4"
      >
        📝 Novidades
      </span>
      <h2 class="text-3xl md:text-4xl font-bold mb-4">
        <%= settings.title || 'Últimas Atualizações' %>
      </h2>
      <p class="text-gray-400 max-w-2xl mx-auto">
        <%= settings.subtitle || 'Acompanhe o progresso do projeto' %>
      </p>
    </div>

    <!-- Updates Grid -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      <% if (data.updates && data.updates.length > 0) { %> <%
      data.updates.slice(0, 3).forEach(function(update) { %>
      <div
        class="glass rounded-xl p-6 border border-white/10 hover:border-primary/50 transition"
      >
        <div class="flex items-center gap-2 mb-3">
          <span
            class="w-2 h-2 rounded-full <%= update.type === 'DONE' ? 'bg-green-500' : update.type === 'IN_PROGRESS' ? 'bg-yellow-500' : 'bg-blue-500' %>"
          ></span>
          <span class="text-xs text-gray-500">
            <%= new Date(update.createdAt).toLocaleDateString('pt-BR', { day:
            '2-digit', month: 'short' }) %>
          </span>
        </div>
        <h3 class="font-semibold mb-2 line-clamp-2"><%= update.title %></h3>
        <p class="text-gray-400 text-sm line-clamp-3"><%= update.content %></p>
      </div>
      <% }); %> <% } else { %>
      <div class="col-span-3 text-center py-8 text-gray-500">
        Nenhuma atualização publicada ainda.
      </div>
      <% } %>
    </div>

    <!-- View All -->
    <div class="text-center mt-8">
      <a
        href="/updates"
        class="inline-flex items-center gap-2 text-primary hover:underline"
      >
        Ver todas as atualizações →
      </a>
    </div>
  </div>
</section>
