<section id="doar" class="py-24">
  <div class="max-w-3xl mx-auto px-6">
    <div class="text-center mb-12">
      <h2 class="text-3xl md:text-4xl font-bold mb-4">
        <%= settings.title || 'Apoie o Projeto' %>
      </h2>
      <p class="text-xl text-gray-400">
        <%= settings.subtitle || 'Sua contribuição mantém o projeto vivo' %>
      </p>
    </div>

    <div class="glass rounded-3xl p-8 border border-white/10">
      <div id="donation-form"><%- include('../donation-form', { config }) %></div>
    </div>
  </div>
</section>
